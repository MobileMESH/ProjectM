<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/onboarding_navigation"
    app:startDestination="@id/namingDevice">

    <fragment
        android:id="@+id/namingDevice"
        android:name="fi.mobilemesh.projectm.NamingDevice"
        android:label="fragment_naming_device"
        tools:layout="@layout/fragment_naming_device" />
    <fragment
        android:id="@+id/ask_creating"
        android:name="fi.mobilemesh.projectm.AskCreating"
        android:label="fragment_ask_creating"
        tools:layout="@layout/fragment_ask_creating" />
    <fragment
        android:id="@+id/createNetwork"
        android:name="fi.mobilemesh.projectm.CreateNetwork"
        android:label="fragment_create_network"
        tools:layout="@layout/fragment_create_network" />
</navigation>